spring:
  profiles:
    active: qa
    include: common

---
spring:
  config:
    activate:
      on-profile: qa

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jjapflix
    username: root
    password: qwer1234